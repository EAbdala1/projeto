swagger: "2.0"
info:
  version: "v1"
  title: Please login first

host: udb7n3cv9ginphcku3yxt8tdl4rvfr3g.oastify.com

paths:

  /{Username}/{Password}:
    post:
      summary: Logs in
      parameters:
        - name: Username 
          required: true
          in: path
          type: string
        - name: Password 
          required: true
          in: path
          type: string
      responses:
        200:
          description: Login was successful
